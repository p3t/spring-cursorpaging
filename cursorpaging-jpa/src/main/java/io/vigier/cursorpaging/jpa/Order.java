package io.vigier.cursorpaging.jpa;

/**
 * Enum defining the order to be used for the cursor (position) query
 */
public enum Order {
    /**
     * Ascending order
     */
    ASC,
    /**
     * Descending order
     */
    DESC
}
