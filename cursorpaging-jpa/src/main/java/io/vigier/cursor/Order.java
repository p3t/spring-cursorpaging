package io.vigier.cursor;

/**
 * Enum defining the order to be used for the cursor (position) query
 */
public enum Order {
    /**
     * Ascending order
     */
    ASC,
    /**
     * Descending order
     */
    DESC
}
